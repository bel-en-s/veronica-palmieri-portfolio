services:
  vero-palmieri-portfolio:
    build: .
    container_name: vero-palmieri-portfolio
    restart: unless-stopped
    expose:
      - "8090"
    volumes:
      - ./pb_data:/app/pb_data
    networks:
      - web

networks:
  web:
    external: true
